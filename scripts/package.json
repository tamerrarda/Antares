{
  "name": "@antares/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Operational scripts. DEV3 owns this package; verify-environment.ts is DEV2's (DEV-PROTOCOL \u00a73).",
  "scripts": {
    "verify-environment": "node --experimental-strip-types verify-environment.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "echo \"(nothing to build)\"",
    "test": "echo \"(no tests yet)\""
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.2"
  }
}
