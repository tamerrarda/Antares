# Screenshots

The images the root `README.md` shows. Three, in this order:

| File | What it should show |
|---|---|
| `vault.png` | The vault page mid-round: the phase, the clock, the auction curve, the action panel |
| `rounds.png` | The round history — what each round sold, how it ended, at what price |
| `claims.png` | A position: shares, pending deposit, queued withdrawal, and what is claimable |

**Take them at 1600×1000 or wider, in the dark theme**, with a real deployment behind them rather
than a mock. A screenshot of a state nobody waited for is a claim about a chain that did not make
it — the same reason the mock app draft was deleted rather than screenshotted.

Nothing here is committed yet; the README leaves the block commented out until it is.
